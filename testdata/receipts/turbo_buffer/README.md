# turbo_buffer live receipts

| file | Machine | backend | placements |
|---|---|---|---|
| `intel-ze.json` | Machine B | Level Zero USM arena | **HOST / SHARED / DEVICE** |

CUDA / Metal arena receipts land here when those hosts prove rent/return
of their placements — never invent numbers, never put lab hostnames in
docs (Machine A / B / C only).
