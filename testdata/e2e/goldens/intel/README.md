# Intel e2e goldens

`minilm.json` was captured on krick-1 (Battlemage) via in-process
OpenVINO GenAI `TextEmbeddingPipeline` on **GPU**. See
`docs/intel-e2e-parity-goldens-krick-1.md` on the parity branch and
`testdata/receipts/turboembed/intel-minilm.json` for the TurboEmbed
C ABI replay (cosine ≥ 0.99).
