# Intel e2e goldens

`minilm.json` was captured on Machine B (Battlemage) via in-process
OpenVINO GenAI `TextEmbeddingPipeline` on **GPU**. See
`docs/intel-e2e-parity-goldens-machine-b.md` on the parity branch and
`testdata/receipts/turboembed/intel-minilm.json` for the TurboEmbed
C ABI replay (cosine ≥ 0.99).
