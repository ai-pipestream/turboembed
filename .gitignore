/target
/.libs/
# Local toolchain leftovers + fetched tokenizer artifacts
/.venv/
.venv-cuda-libs/
# Fetched model artifacts stay out of git — but the SHA-256 manifests are
# committed (cargo run -p inferstream-fetch verifies downloads against them).
/models/*
!/models/manifests/
.DS_Store
