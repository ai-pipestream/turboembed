# Test run record, 2026-09-21 (RTX 4080 SUPER host)

Run by a Sonnet agent on an x86_64 host (AMD Ryzen 9 9950X3D, RTX 4080
SUPER, driver 595.84) while the tree moved from `55820b3` to `4ec809b`; each step
ran against the tree as it was at that moment. Raw logs were kept outside
the repository. Toolchain: rustc 1.97.1, nvcc 12.4, OpenJDK 25.0.3
(Temurin), Maven 3.9.16, OpenVINO 2026.3.1.

| Step | Command | Result | Wall time |
|---|---|---|---|
| 1a | `cargo fmt --all -- --check` | clean | 0.15 s |
| 1b | `cargo clippy --locked --workspace --exclude turbo-provider-cuda --all-targets -- -D warnings` | clean | 33 s |
| 1c | `cargo test --locked --workspace --exclude turbo-provider-cuda` | 45 suites, all `0 failed` | 32 s |
| 1d | `scripts/gen-header.sh --check` | headers up to date | 0.2 s |
| 1e | `scripts/gen-versioned.py --check` | clean | 0.04 s |
| 1f | `cargo run -p turbo-core --example write_mock_bundles` then `git diff --exit-code -- testdata/bundles` | fixtures unchanged | 0.1 s |
| 1g | `scripts/c-smoke.sh` | `c-smoke: OK` | 0.5 s |
| 1h | `scripts/package.sh --no-cuda --no-openvino` | archive `dist/turbo-2.0.0-alpha.0-x86_64-unknown-linux-gnu.tar.gz` (89 MB); embedded C smoke OK | 14 s |
| 2 | CUDA provider unit tests (`cargo test -p turbo-provider-cuda`) | 4 + 12 + 8 + 2 passed | 4.5 s |
| 3 | CUDA live `live_embed` + `live_tasks`, ordinal 0 | 13 + 7 passed | 2 m 14 s |
| 4a | OpenVINO live `live_embed` + `live_tasks`, CPU | 13 + 7 passed | 55 s |
| 4b | OpenVINO live, GPU (ordinal 0) | 13 + 7 passed | 1 m |
| 4c | `build/openvino/turbo_provider_openvino_test`, CPU | 10 tests, 0 failed checks | 3.7 s |
| 4d | same, GPU | 10 tests, 0 failed checks | 4.7 s |
| 5a | ggml `live_generate`, CPU | 7 passed | 3 m 2 s |
| 5b | ggml `live_generate`, GPU | 7 passed | 3 m |
| 6 | `bindings/java`: `mvn -q -B test` | 9 tests, 0 failures | 1.3 s |

No test failed and every process exited 0.

Observation recorded for follow-up: `scripts/package.sh --no-cuda
--no-openvino` skips only the *build* of those providers and still packages
an already built `libturbo_provider_cuda.so` and
`libturbo_provider_openvino.so` found under `target/`, so the archive was
not free of them despite the flags.
