/* Header-only Clang module. Function symbols come from
 * `TurboEmbed` (`@_cdecl`) on Apple or `native/turboembed/src/stub.cpp`
 * when the Rust crate / C++ stub is the implementation. */
#include "turboembed.h"
