# Local embed-parity serve list (not a catalog change).
# minilm + bge-small on ORT CUDA — enough for e2e-parity goldens.
listen = "0.0.0.0:8461"

serve = [
    "minilm",
    "bge-small",
]

[auth]
mode = "bearer"
bearer_tokens = ["change-me"]

[[models]]
name = "mock-embed"
backend = "mock"
